language: android

jdk:
  - oraclejdk8

android:
  components:
    - tools
    - platform-tools
    - android-25
    - build-tools-25.0.1

# cache between builds
cache:
  directories:
  - $HOME/.m2
  - $HOME/.gradle